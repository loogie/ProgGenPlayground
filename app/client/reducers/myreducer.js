export default function myreducer(state={}, action){
    switch(action.type){
        //case "CONFIG":
        //return Object.assign({}, state, {config: action.config});
        default:
        return state;
    }
}
  